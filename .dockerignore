# Rust build artifacts
target/
**/*.rs.bk

# Git and GitHub
.git/
.github/
.gitignore

# Docker files (no need to include in build context)
Dockerfile
docker-compose.yml
.dockerignore

# Temporary files
*.tmp
*.log
